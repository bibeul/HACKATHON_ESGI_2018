<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="al.esgi.com.solistrophe">

    <application
        android:banner="@drawable/logo"
        android:logo="@drawable/solistrophe"
        android:allowBackup="true"
        android:icon="@drawable/solistrophe"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar" >
        <activity android:name=".login.Signin" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.Register"></activity>
    </application>

</manifest>